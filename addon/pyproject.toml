[project]
name = "notatki"
version = "0.0.0"
description = "Notatki text notes importer addon for Anki"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "mistune>=3.2.0",
]

[dependency-groups]
dev = [
  "aqt>=25.9.2",
  "mypy>=1.19.1",
  "pytest>=9.0.2",
]
