{
  "private": true,
  "name": "@notatki/examples",
  "version": "0.0.0",
  "scripts": {
    "insert-id": "notatki insert-id",
    "reformat": "notatki reformat",
    "export": "notatki export --csv --json",
    "build": "npm run insert-id && npm run reformat && npm run export"
  }
}
